# 安全与隐私说明

## 密码

- PDF 密码仅作为 `pypdf.PdfReader.decrypt()` 的函数参数使用。
- 应用不会把密码写入日志、导出文件、数据库或本地临时文件。
- Streamlit 控件值会在当前浏览器会话对应的服务进程内短暂存在；因此远程部署不等于“密码从未离开用户设备”。

## 文件

- 默认以内存字节流完成上传、解密、解析、ZIP 和 Excel 生成。
- Git 仓库通过 `.gitignore` 排除 PDF、Excel、ZIP、`.env` 和 Streamlit secrets。
- 不建议在公共云上处理未公开或涉密申请书。

## AI 调用

- AI 功能默认关闭，只有用户主动提供 API Key 或服务器配置密钥时才调用。
- 发送内容为结构化字段与必要正文区块，不发送 PDF 文件本体。
- 对敏感材料应关闭外部 AI；可在后续版本接入本地模型。

## 推荐部署

- 个人使用：本机运行。
- 课题组/单位使用：内网 Docker 或受控服务器。
- 公开演示：只使用脱敏样例。

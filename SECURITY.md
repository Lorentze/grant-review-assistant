# 数据安全说明

## 不持久化的敏感信息

应用不会将以下内容写入任何输出文件：

- PDF 密码；
- OpenAI API Key；
- Ollama 无需密钥；
- 用户在文件选择框中的临时密码状态。

密码只作为一次函数调用的参数用于解密。处理完成后，前端密码框被清空，后端不将其存入对象状态。

## 本地文件

应用只读取用户主动选择的 PDF，只向用户主动选择的输出目录写入：

- 解密 PDF；
- Excel 汇总；
- 批次 JSON。

“清除当前批次”仅清除应用内存，不会删除已经输出的本地文件。

## 外部 AI

默认规则模式不访问网络。OpenAI 模式必须由用户主动选择并确认，发送范围限制为结构化字段与必要正文片段。PDF 二进制内容与 PDF 密码不发送。

## 日志

正式打包默认关闭控制台和调试日志。开发者只有设置 `GRANT_REVIEW_DEBUG=1` 时才会输出异常堆栈；异常信息中不应包含密码或 API Key。

## 建议

- 未公开材料优先使用本地规则或本地 Ollama。
- 正式分发前对 Windows 和 macOS 应用进行代码签名。
- 处理极高敏感材料时，使用受控电脑并关闭外部 AI 模式。
